import React from "react";
import styles from './EachItem.module.css';

const EachItem = () => {
    return (
        <div className={`${styles['each-item']}`}> 
            
        </div>
    )
};

export default EachItem;
